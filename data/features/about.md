## Dataset

After adding raw data files into `data/raw` and running `src/data/split_data.py`; four files `data.csv`, `train.csv`, `test.csv` and `val.csv` will be saved in this directory.

Dataset is used for **educational and research purposes**.