## Dataset

After adding raw data files into `data/raw` and running `src/data/split_data.py`; two files `application_record_processed.csv` and `credit_record_processed.csv` will be saved in this directory.

Dataset is used for **educational and research purposes**.